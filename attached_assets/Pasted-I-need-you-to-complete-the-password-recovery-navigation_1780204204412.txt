I need you to complete the password recovery navigation fix in Beluga Fit.

CURRENT STATE (already correct on GitHub main):
- AuthContext applyRecoveryUrl() calls setSession() then directly sets setIsPasswordRecovery(true)
- isPasswordRecovery is exported in the AuthContext value
- updatePassword() resets isPasswordRecovery to false on success
- ForgotPasswordScreen exists and triggers resetPassword correctly

WHAT IS MISSING:
No screen in the app currently watches isPasswordRecovery and navigates to a change-password UI.
After the deep link opens the app, isPasswordRecovery becomes true but nothing happens with it.
The user lands straight on the home/dashboard instead of being shown a "Set new password" screen.

YOUR TASKS:

1. Create src/components/ChangePasswordScreen.tsx
   - Import useAuth from ../contexts/AuthContext
   - Import useNavigation from @react-navigation/native
   - Two fields: newPassword and confirmPassword (both TextInput with secureTextEntry)
   - Submit button: validate both match and are non-empty, then call updatePassword(newPassword)
   - On success: isPasswordRecovery is already reset to false by AuthContext, navigate to Home
   - On error: show an error box matching the app's existing error style
   - On loading: show ActivityIndicator
   - Style it to match ForgotPasswordScreen visual design

2. Wire navigation into the existing navigation structure
   - Find where your screens are registered (App.tsx, _layout, or navigator config)
   - Register ChangePasswordScreen so it is reachable by name
   - The route name should be "ChangePassword"

3. Make the home UI react to isPasswordRecovery
   - Find the screen or layout component that renders after auth resolves (Home, Dashboard, or root layout)
   - Import useAuth and read isPasswordRecovery
   - Add useEffect that watches isPasswordRecovery
   - When isPasswordRecovery becomes true, immediately navigate.push/replace to ChangePasswordScreen
   - Do NOT navigate to Home until isPasswordRecovery is false

4. DO NOT change any AuthContext logic, deep-link parsing, or setSession behavior.
   Only add the navigation layer that was missing.

5. Commit with message: fix: add ChangePasswordScreen and wire isPasswordRecovery navigation