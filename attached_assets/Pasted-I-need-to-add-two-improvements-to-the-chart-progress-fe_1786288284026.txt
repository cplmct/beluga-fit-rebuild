I need to add two improvements to the chart/progress features in Beluga Fit.

Read both files fully before making any changes.

FILES TO MODIFY:
1. src/components/ExerciseDetailScreen.tsx
2. src/components/BodyTrackerScreen.tsx

react-native-chart-kit and react-native-svg are already installed.
LineChart is already imported and used in BodyTrackerScreen.
Do NOT install any new packages.

---

TASK 1 — ExerciseDetailScreen.tsx

Add a weight progression line chart above the existing 
"Weight Progression" text list section.

The sessionHistory state already contains all the data needed:
- sessionHistory[i].date — ISO date string
- sessionHistory[i].sets[0].weightKg — weight of first set

DATA PREPARATION:
- Filter sessionHistory to only sessions where sets[0].weightKg is not null
- Take last 10 sessions maximum
- Sort ascending by date (oldest first — left to right on chart)
- X axis labels: formatShortDate(item.date) — e.g. "Jul 28"
- Y axis values: sets[0].weightKg (the weight lifted that session)
- Only show the chart if there are 2 or more data points

CHART CONFIG (match BodyTrackerScreen style exactly):
import { LineChart } from 'react-native-chart-kit';
import { Dimensions } from 'react-native'; // already imported

const screenWidth = Dimensions.get('window').width;

chartConfig = {
  backgroundColor: '#ffffff',
  backgroundGradientFrom: '#ffffff',
  backgroundGradientTo: '#ffffff',
  decimalPlaces: 1,
  color: (opacity = 1) => `rgba(37, 99, 235, ${opacity})`,
  labelColor: (opacity = 1) => `rgba(100, 116, 139, ${opacity})`,
  style: { borderRadius: 12 },
  propsForDots: {
    r: '4',
    strokeWidth: '2',
    stroke: '#2563eb',
  },
}

PLACEMENT:
Replace the existing plain text "Weight Progression" list 
section with this enhanced version:

1. Section label "WEIGHT PROGRESSION" (existing style)
2. IF 2+ data points: show LineChart in a white card with 
   border radius 14, border color #e2e8f0, padding 12
3. Below the chart (or instead of chart if < 2 points): 
   keep the existing progression row list unchanged
   (the date + weight × reps rows already in the file)

NEW STYLES to add:
chartCard: {
  backgroundColor: '#ffffff',
  borderRadius: 14,
  borderWidth: 1,
  borderColor: '#e2e8f0',
  padding: 12,
  marginBottom: 16,
  overflow: 'hidden',
}
chartTitle: {
  fontSize: 12,
  fontWeight: '700',
  color: '#64748b',
  marginBottom: 8,
}

---

TASK 2 — BodyTrackerScreen.tsx

The existing charts only show weight and waist trends.
Add 3 more measurement trend charts:

1. Left Arm trend (field: 'left_arm')
2. Hips trend (field: 'hips')  
3. Left Thigh trend (field: 'left_thigh')

The existing prepareChartData function already accepts 
'weight' | 'waist' — extend it to also accept:
'left_arm' | 'hips' | 'left_thigh'

BEFORE:
const prepareChartData = (field: 'weight' | 'waist') => {

AFTER:
const prepareChartData = (
  field: 'weight' | 'waist' | 'left_arm' | 'hips' | 'left_thigh'
) => {

Add three new chart variables after the existing ones:
const armChartData = prepareChartData('left_arm');
const hipsChartData = prepareChartData('hips');
const thighChartData = prepareChartData('left_thigh');

Add three new chart JSX blocks after the existing waist chart,
following the exact same pattern:

{armChartData && (
  <View style={styles.chartCard}>
    <Text style={styles.chartTitle}>Arm Trend ({lengthUnit})</Text>
    <LineChart
      data={armChartData}
      width={screenWidth - 40}
      height={200}
      chartConfig={chartConfig}
      bezier
      style={styles.chart}
      withInnerLines={false}
    />
  </View>
)}

{hipsChartData && (
  <View style={styles.chartCard}>
    <Text style={styles.chartTitle}>Hips Trend ({lengthUnit})</Text>
    <LineChart
      data={hipsChartData}
      width={screenWidth - 40}
      height={200}
      chartConfig={chartConfig}
      bezier
      style={styles.chart}
      withInnerLines={false}
    />
  </View>
)}

{thighChartData && (
  <View style={styles.chartCard}>
    <Text style={styles.chartTitle}>Thigh Trend ({lengthUnit})</Text>
    <LineChart
      data={thighChartData}
      width={screenWidth - 40}
      height={200}
      chartConfig={chartConfig}
      bezier
      style={styles.chart}
      withInnerLines={false}
    />
  </View>
)}

---

EXECUTION ORDER:
1. Show me the plan for Task 1 (ExerciseDetailScreen chart) 
   with exact line numbers before applying
2. Show me the plan for Task 2 (BodyTrackerScreen additions)
   with exact line numbers before applying
3. Apply Task 1 — run tsc --noEmit — confirm clean
4. Apply Task 2 — run tsc --noEmit — confirm clean
5. Show line counts for both files
6. Do NOT commit — I will handle git myself

Show Task 1 plan first.